import 'package:flutter/material.dart';

class ExspensesScreen extends StatelessWidget {
  const ExspensesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}