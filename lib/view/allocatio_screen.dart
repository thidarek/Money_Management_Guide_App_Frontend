import 'package:flutter/material.dart';

class AllocatioScreen extends StatelessWidget {
  const AllocatioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}